import Handlebars = require('handlebars');


describe('Feedback dialog template', () => {

    beforeEach(() => {

    });

    it('should have a template', () => {
        //var dialogTemplate = Handlebars.templates['feedback_dialog'];

        console.log(Handlebars.templates);
    });
});

