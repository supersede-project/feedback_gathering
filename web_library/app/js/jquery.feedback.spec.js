define(["require", "exports"], function (require, exports) {
    "use strict";
    xdescribe('jQuery Validate Plugin', function () {
        var $ = $j;
        beforeEach(function () {
        });
        it('should initialize the translations', function () {
        });
        it('should load the data from the orchestrator', function () {
        });
        it('should set the background color of the element to the given color or the default one', function () {
        });
        it('should set the text color of the element to the given color or the default one', function () {
        });
        it('should add a click event on the element', function () {
        });
        it('should initialize the push mechanism dialog', function () {
        });
        it('should trigger maximally one pull configuration at a time', function () {
        });
        it('should initialize the template with a given configuration', function () {
        });
        it('should initialize an intermediate dialog', function () {
        });
        it('should initialize the rating mechanism', function () {
        });
        it('should initialize the screenshot mechanism', function () {
        });
        it('should initialize the dialog', function () {
        });
        it('should add some events', function () {
        });
        it('should prepare the form data', function () {
        });
        it('should toggle the dialog', function () {
        });
        it('should open the dialog', function () {
        });
        it('should reset the message view', function () {
        });
    });
});
//# sourceMappingURL=jquery.feedback.spec.js.map