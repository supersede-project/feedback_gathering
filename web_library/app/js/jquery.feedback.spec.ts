import {feedbackPluginModule} from './jquery.feedback';
import Handlebars = require('handlebars');


xdescribe('jQuery Validate Plugin', () => {
    let $ = $j;

    beforeEach(() => {
        //feedbackPluginModule($, window, document);
    });

    it('should initialize the translations', () => {

    });

    it('should load the data from the orchestrator', () => {

    });

    it('should set the background color of the element to the given color or the default one', () => {

    });

    it('should set the text color of the element to the given color or the default one', () => {

    });

    it('should add a click event on the element', () => {

    });

    it('should initialize the push mechanism dialog', () => {

    });

    it('should trigger maximally one pull configuration at a time', () => {

    });

    it('should initialize the template with a given configuration', () => {

    });

    it('should initialize an intermediate dialog', () => {

    });

    it('should initialize the rating mechanism', () => {

    });

    it('should initialize the screenshot mechanism', () => {

    });

    it('should initialize the dialog', () => {

    });

    it('should add some events', () => {

    });

    it('should prepare the form data', () => {

    });

    it('should toggle the dialog', () => {

    });

    it('should open the dialog', () => {

    });

    it('should reset the message view', () => {

    });
});

